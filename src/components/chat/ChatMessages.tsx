import React, { Component } from "react";
import { Message } from "./CurrentChat";

type MessagesProps = {
    messages: Message[];
}
type MessagesState = {
    messages: Message[];
}
export class ChatMessages extends Component<MessagesProps, MessagesState> {
    

}